{
  "name": "studio",
  "version": "0.0.1",
  "description": "NCE Studio",
  "private": true,
  "scripts": {
    "build": "cd frontend && yarn install && yarn build",
    "dev": "cd frontend && yarn dev"
  },
  "workspaces": [
    "frontend"
  ],
  "overrides": {
    "@iconify/utils": "2.3.0"
  },
  "resolutions": {
    "@iconify/utils": "2.3.0",
    "dompurify": "3.1.7"
  }
}
