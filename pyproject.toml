[project]
name = "studio"
version = "0.0.1"
description = "NCE Studio"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "NCE Studio", email = "developer@example.com" },
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<17.0.0"

# Uncomment if your app also depends on ERPNext:
# erpnext = ">=15.0.0,<17.0.0"
